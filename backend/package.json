{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "cd .. && ./backend/.venv/bin/python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload",
    "test": "cd .. && ./backend/.venv/bin/python -m unittest discover -s backend/tests -t . -v",
    "cli": "python3 cli.py",
    "openai:smoke": "cd .. && ./backend/.venv/bin/python backend/scripts/openai_tool_visibility_smoke.py"
  }
}
