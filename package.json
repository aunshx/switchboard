{
  "name": "switchboard",
  "private": true,
  "packageManager": "pnpm@11.15.0",
  "scripts": {
    "dev": "pnpm --filter backend dev",
    "build": "turbo run build",
    "cli": "python3 -m backend.cli",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "backend:cli": "python3 -m backend.cli",
    "frontend:dev": "pnpm --filter frontend dev",
    "backend:dev": "pnpm --filter backend dev"
  },
  "devDependencies": {
    "turbo": "^2.9.5"
  }
}
